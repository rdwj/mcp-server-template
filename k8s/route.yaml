apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: fastmcp-unified
  namespace: mcp
spec:
  to:
    kind: Service
    name: fastmcp-unified
  port:
    targetPort: http
  tls:
    termination: edge
